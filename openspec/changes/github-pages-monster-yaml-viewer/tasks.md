## 1. Репозиторий и данные

- [x] 1.1 Зафиксировать структуру YAML (один `data/monsters.yaml` с `monsters[]` или каталог `data/monsters/`) и добавить пример записей с полями `id`, `name`.
- [x] 1.2 Описать в README расположение файлов и правила для контрибьюторов.

## 2. Сборка каталога YAML → JSON

- [x] 2.1 Добавить скрипт сборки (Node), который читает YAML, проверяет уникальность `id`, пишет `monsters.json` (или эквивалент) в каталог, отдаваемый статикой.
- [x] 2.2 Подключить скрипт к `npm run build` перед или вместе со сборкой фронтенда.

## 3. Статическое приложение (UI)

- [x] 3.1 Инициализировать проект (Vite + TypeScript), настроить `base` через переменную окружения для GitHub Pages.
- [x] 3.2 Загрузить каталог JSON с тем же origin; отобразить список монстров и вид с полями записи (минимум `id`, `name`).
- [x] 3.3 Реализовать обработку ошибки загрузки/парсинга с понятным сообщением в UI.

## 4. CI и GitHub Pages

- [x] 4.1 Добавить workflow GitHub Actions: установка зависимостей, `id`-проверка и сборка, публикация артефакта в Pages (ветка/режим по документации GitHub).
- [x] 4.2 Задокументировать в README включение Pages (источник: GitHub Actions) и значение base path для репозитория.

## 5. Проверка

- [x] 5.1 Локально проверить `build` с `base`, соответствующим имени репо.
- [x] 5.2 После merge убедиться, что деплой открывается по URL Pages и каталог отображается.
