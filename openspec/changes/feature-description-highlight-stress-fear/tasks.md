## 1. Рендер описания фичи

- [x] 1.1 Добавить функцию (или локальный хелпер), которая по строке `description` возвращает сегменты «текст / ключевое слово» для токенов `Stress` и `Fear` с границами слова (`\b`), без изменения регистра совпадений.
- [x] 1.2 В `FeatureBlock` заменить единичный `Text` с `{f.description}` на рендер сегментов: обычный текст — `Text span` как сейчас; для ключевых слов — `Text span` с `fw={700}` и `fs="italic"` (или эквивалент Mantine).
- [x] 1.3 Убедиться, что контейнер описания по-прежнему задаёт `whiteSpace: "pre-wrap"` (на обёртке или родителе), чтобы многострочные описания не ломались.

## 2. Проверка

- [x] 2.1 Вручную открыть карточку с текстом вроде «Mark a Stress» и «gain a Fear» и убедиться в подсветке и отсутствии поломки переносов.
- [x] 2.2 Прогнать `npm run build` (или существующий CI-эквивалент) без ошибок.
