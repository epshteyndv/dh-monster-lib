## 1. Схема и данные

- [x] 1.1 Зафиксировать TypeScript-типы карточки (`stats`, `attack`, `features[]`) в `src/types.ts` (и при необходимости общий модуль для скрипта сборки).
- [x] 1.2 Переписать `data/monsters.yaml` под новую схему; добавить хотя бы одну запись, отражающую структуру примера карточки (Acid Burrower или эквивалент).

## 2. Валидация сборки

- [x] 2.1 Расширить `scripts/build-catalog.mjs`: проверка обязательных полей, типов, `features[].type`, дубликатов `id`; понятные сообщения об ошибках.
- [x] 2.2 Убедиться, что при ошибке валидации процесс завершается с ненулевым кодом.

## 3. UI (Mantine)

- [x] 3.1 Заменить отображение «плоского» списка полей на карточку: шапка (имя, tier, flavor, motives), блок статов (difficulty, thresholds, hp, stress, строка атаки), experience, секция Features с типами passive/action/reaction.
- [x] 3.2 Сохранить список монстров слева (или аналог) и выбор записи; адаптивная вёрстка при необходимости.

## 4. Документация и проверка

- [x] 4.1 Обновить `README.md`: описание полей YAML для авторов данных.
- [x] 4.2 Выполнить `npm run build` и визуально проверить карточку в dev/preview.
