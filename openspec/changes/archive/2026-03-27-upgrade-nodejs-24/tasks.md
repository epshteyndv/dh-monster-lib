## 1. CI

- [x] 1.1 В `.github/workflows/deploy-pages.yml` заменить `node-version: "20"` на `node-version: "24"`.

## 2. Репозиторий и документация

- [x] 2.1 Добавить в корень `.nvmrc` со строкой `24`.
- [x] 2.2 В `package.json` добавить `engines.node` (например `>=24.0.0` или `^24.0.0`) в соответствии с политикой проекта.
- [x] 2.3 Обновить `README.md`: указать Node.js 24 как рекомендуемую версию для локальной разработки.

## 3. Проверка

- [x] 3.1 Переключиться на Node 24 локально, выполнить `npm ci` и `npm run build`; при необходимости обновить `package-lock.json`.
- [x] 3.2 После merge убедиться, что workflow на GitHub Actions проходит успешно.
